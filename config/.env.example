# Django Core
SECRET_KEY=
DEBUG=
ALLOWED_HOSTS=

# Database (PostgreSQL for production, SQLite for dev)
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# Email (Brevo SMTP for production)
BREVO_SMTP_HOST=
BREVO_SMTP_PORT=
BREVO_SMTP_USER=
BREVO_SMTP_PASSWORD=
DEFAULT_FROM_EMAIL=
ADMIN_EMAIL=